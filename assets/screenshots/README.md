# Screenshots

Run `python scripts/capture_screenshots.py` to launch the local Streamlit application and capture fully synthetic views.

The checked-in PNG files are explicitly labelled placeholders because Chromium navigation to the local Streamlit URL was blocked by the build environment's administrative browser policy. The application itself passed its local health-endpoint smoke test. Running the capture script on an unrestricted local machine replaces the placeholders with genuine application screenshots.
