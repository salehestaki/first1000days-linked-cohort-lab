# Screenshots

Run `python scripts/capture_screenshots.py` to launch the local Streamlit application and capture fully synthetic views.

If browser automation is blocked, the script now creates clean preview cards instead of dumping browser-error text into the checked-in PNGs. Running the capture script on an unrestricted local machine replaces those preview cards with genuine application screenshots.
