# Linkage rulebook

The executable rule registry is written to `data/linkage_rulebook.csv`. Rules have stable IDs, severity, source table, rationale, remediation, and analysis-blocking status. Families cover primary keys, foreign keys, parent roles, child-parent family consistency, chronology, sibling structure, educational repeats, outcome consistency, and exposure-row integrity. Source data are never silently repaired.
