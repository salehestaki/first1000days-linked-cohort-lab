# User guide

Install dependencies, run `streamlit run app.py`, and navigate through the seven pages. Use the sidebar to select clean or corrupted synthetic data, filter regions and area contexts, choose exposure windows and parent roles, regenerate deterministic data, run all reports, and download aggregate outputs. Never upload real records.
